mixin link(url, label)
  div
    a(href=url)= label

mixin navigation
  div
    +link("/languages", "Manage languages")
    +link("/upload", "Upload card images")
    +link("/assign-images", "Assign images to cards")
    +link("/set-emissions", "Set card emissions")
    +link("/assign-language", "Assign cards to languages")
    +link("/cards", "Edit cards")
